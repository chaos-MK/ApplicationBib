apiVersion: v1
kind: ServiceAccount
metadata:
  name: postgres-azure-sa
  namespace: applicationbib
  annotations:
    azure.workload.identity/client-id: "${AZ_POSTGRES_CLIENT_ID}"
automountServiceAccountToken: true
