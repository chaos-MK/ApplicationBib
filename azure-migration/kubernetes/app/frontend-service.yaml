apiVersion: v1
kind: Service
metadata:
  name: ritual-growth-ui
  namespace: applicationbib
  labels:
    app: ritual-growth-ui
spec:
  type: ClusterIP
  selector:
    app: ritual-growth-ui
  ports:
    - name: http
      port: 3000
      targetPort: 3000
